{% extends 'base.html.twig' %}

{% block title %}UserImages{% endblock %}
{% block homeActive %}{% endblock %}

{% block body %}
    <h1>UserImages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_image.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_image.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_images_index') }}">back to list</a>

    <a href="{{ path('app_user_images_edit', {'id': user_image.id}) }}">edit</a>

    {{ include('user_images/_delete_form.html.twig') }}
{% endblock %}
